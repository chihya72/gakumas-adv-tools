# 快速开始

5 分钟快速上手 gakumas-adv-tools！

## 📦 安装

### 前置要求
- Python 3.8+
- Node.js 16+ (如果使用 Editor)
- Git

### 克隆项目（包含数据）

```bash
# 克隆项目并自动初始化 submodule
git clone --recursive https://github.com/chihya72/gakumas-adv-tools.git
cd gakumas-adv-tools

# 如果已经克隆但没有 submodule
git submodule update --init --recursive
```

### 安装 Python 依赖

```bash
# 创建虚拟环境（推荐）
python -m venv .venv

# 激活虚拟环境
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

## 🚀 快速体验

### 1. 分析面部表情使用情况

```bash
python analyze_facial_indices.py
```

**输出**：
- 控制台显示统计结果
- 生成 `gakumas-data/facial_index_analysis.txt` 详细报告

### 2. 解析单个 ADV 脚本

```bash
cd parser
python parser.py
```

**输出**：
- 解析 `gakumas-data/data/adv_cidol-amao-3-000_01.txt`
- 生成 JSON 格式输出
- 显示对话摘要和时间轴信息

### 3. 批量解析所有脚本

```bash
cd parser
python batch_parser.py
```

**输出**：
- 解析所有 2962 个 ADV 文件
- 输出到 `output/` 目录
- 生成统计报告

### 4. 启动可视化编辑器

```bash
cd editor
npm install
npm run dev
```

**访问**: http://localhost:5173

**功能**：
- 可视化编辑 ADV 脚本
- 实时预览对话
- 时间轴编辑器
- 资源选择器

### 5. 启动资源数据库 API（可选）

```bash
cd database
python resource_api_server.py
```

**API 地址**: http://localhost:5000

## 📖 下一步

- 📘 阅读 [actorfacialoverridemotion](actorfacialoverridemotion) 了解面部表情命令
- 🎭 查看 [表情 Index 参考表](表情-Index-参考表) 查询已验证的表情索引
- 💡 学习 [表情编辑实战](表情编辑实战) 开始修改表情

## 🆘 常见问题

### Q: 找不到数据文件？
**A**: 确保已正确初始化 submodule：
```bash
git submodule update --init --recursive
```

### Q: Parser 报错找不到文件？
**A**: 检查 `gakumas-data/data/` 目录是否存在且包含 `.txt` 文件。

### Q: 如何更新数据？
**A**: 更新 submodule 到最新版本：
```bash
cd gakumas-data
git pull origin master
cd ..
git add gakumas-data
git commit -m "Update data"
```

## 🔗 更多资源

- [项目架构](项目架构) - 理解代码结构
- [命令参考](Home#命令参考) - 完整命令列表
- [GitHub Issues](https://github.com/chihya72/gakumas-adv-tools/issues) - 报告问题
